[project]
name = "exoscale-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "exoscale>=0.12.0",
    "mcp[cli]>=1.10.1",
]
